---
interface Props {
  items?: string[];
}

const {
  items = [
    'Site Under Construction'
  ],
} = Astro.props;
---

<marquee width="100%" direction="left">
  {items.map((text) => <span style={{ color: '#FF0000', fontWeight: 'bold' }}>{text}</span>)}
</marquee>
